local admins = {
    "s3r31n_n", "RaaneshGamer4435K", "Mvspro12321", "DxxrkVenom", "RealGloxh", "vicekeeper",
    "SempaiH9", "24l05l09", "coolkidsuper500", "S1verlynne", "cclarify", "nukestar", "swagknife19",
    "beodnt", "Sama_roblox34", "1Daxito", "Juanpith0rik0", "FreedomBirth", "dytona_x", "Souligraphy",
    "stuckwithpoli", "Imjustaproguy2232", "OmegaAnoobis", "NotVpxr", "lichugamerpro", "Brockkodile",
    "Atavi", "ZacksFy", "BossSpax", "yHasteeD", "WtrMelonAdam", "FamedChris", "DBS_WorkflowBot",
    "dbs_johnny", "decim8or", "EnlocK", "DBSRedis", "BrickStormer01", "Blade_Executive"
}

local ScreenGui = Instance.new("ScreenGui")
local MainFrame = Instance.new("Frame")
local TitleBar = Instance.new("Frame")
local Title = Instance.new("TextLabel")
local CloseButton = Instance.new("TextButton")
local Status = Instance.new("TextLabel")
local ToggleButton = Instance.new("TextButton")
local AdminCount = Instance.new("TextLabel")
local Corner = Instance.new("UICorner")
local TitleCorner = Instance.new("UICorner")
local ButtonCorner = Instance.new("UICorner")

ScreenGui.Parent = game.CoreGui
ScreenGui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling

MainFrame.Parent = ScreenGui
MainFrame.BackgroundColor3 = Color3.fromRGB(20, 20, 20)
MainFrame.BorderSizePixel = 0
MainFrame.Position = UDim2.new(0, 20, 0, 100)
MainFrame.Size = UDim2.new(0, 250, 0, 150)
MainFrame.Active = true
MainFrame.Draggable = true

TitleBar.Parent = MainFrame
TitleBar.BackgroundColor3 = Color3.fromRGB(40, 40, 40)
TitleBar.BorderSizePixel = 0
TitleBar.Position = UDim2.new(0, 0, 0, 0)
TitleBar.Size = UDim2.new(0, 250, 0, 30)

Title.Parent = TitleBar
Title.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
Title.BackgroundTransparency = 1
Title.BorderSizePixel = 0
Title.Position = UDim2.new(0, 10, 0, 0)
Title.Size = UDim2.new(0, 200, 0, 30)
Title.Font = Enum.Font.GothamBold
Title.Text = "Admin Detector"
Title.TextColor3 = Color3.fromRGB(255, 255, 255)
Title.TextSize = 14
Title.TextXAlignment = Enum.TextXAlignment.Left

CloseButton.Parent = TitleBar
CloseButton.BackgroundColor3 = Color3.fromRGB(255, 50, 50)
CloseButton.BorderSizePixel = 0
CloseButton.Position = UDim2.new(0, 220, 0, 5)
CloseButton.Size = UDim2.new(0, 20, 0, 20)
CloseButton.Font = Enum.Font.SourceSans
CloseButton.Text = "×"
CloseButton.TextColor3 = Color3.fromRGB(255, 255, 255)
CloseButton.TextSize = 16

Status.Parent = MainFrame
Status.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
Status.BackgroundTransparency = 1
Status.BorderSizePixel = 0
Status.Position = UDim2.new(0, 10, 0, 40)
Status.Size = UDim2.new(0, 230, 0, 20)
Status.Font = Enum.Font.Gotham
Status.Text = "Estado: Monitoreando..."
Status.TextColor3 = Color3.fromRGB(85, 255, 85)
Status.TextSize = 12
Status.TextXAlignment = Enum.TextXAlignment.Left

AdminCount.Parent = MainFrame
AdminCount.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
AdminCount.BackgroundTransparency = 1
AdminCount.BorderSizePixel = 0
AdminCount.Position = UDim2.new(0, 10, 0, 65)
AdminCount.Size = UDim2.new(0, 230, 0, 20)
AdminCount.Font = Enum.Font.Gotham
AdminCount.Text = "Admins en lista: " .. #admins
AdminCount.TextColor3 = Color3.fromRGB(200, 200, 200)
AdminCount.TextSize = 12
AdminCount.TextXAlignment = Enum.TextXAlignment.Left

ToggleButton.Parent = MainFrame
ToggleButton.BackgroundColor3 = Color3.fromRGB(85, 170, 255)
ToggleButton.BorderSizePixel = 0
ToggleButton.Position = UDim2.new(0, 75, 0, 100)
ToggleButton.Size = UDim2.new(0, 100, 0, 30)
ToggleButton.Font = Enum.Font.GothamBold
ToggleButton.Text = "Desactivar"
ToggleButton.TextColor3 = Color3.fromRGB(255, 255, 255)
ToggleButton.TextSize = 12

Corner.Parent = MainFrame
TitleCorner.Parent = TitleBar
ButtonCorner.Parent = ToggleButton
Corner.CornerRadius = UDim.new(0, 5)
TitleCorner.CornerRadius = UDim.new(0, 5)
ButtonCorner.CornerRadius = UDim.new(0, 3)

local isActive = true
local checkInterval = 2

local function isAdmin(playerName)
    for _, adminName in ipairs(admins) do
        if adminName == playerName then
            return true
        end
    end
    return false
end

local function selfKick()
    game.Players.LocalPlayer:Kick("Administrador detectado - Auto-expulsión activada")
end

local function checkForAdmins()
    if not isActive then return end
    
    for _, player in ipairs(game.Players:GetPlayers()) do
        if isAdmin(player.Name) then
            Status.Text = "¡Admin detectado: " .. player.Name .. "!"
            Status.TextColor3 = Color3.fromRGB(255, 85, 85)
            wait(1)
            selfKick()
            return
        end
    end
end

game.Players.PlayerAdded:Connect(function(player)
    if not isActive then return end
    
    if isAdmin(player.Name) then
        Status.Text = "¡Admin entró: " .. player.Name .. "!"
        Status.TextColor3 = Color3.fromRGB(255, 85, 85)
        wait(1)
        selfKick()
    end
end)

ToggleButton.MouseButton1Click:Connect(function()
    isActive = not isActive
    if isActive then
        ToggleButton.Text = "Desactivar"
        ToggleButton.BackgroundColor3 = Color3.fromRGB(85, 170, 255)
        Status.Text = "Estado: Monitoreando..."
        Status.TextColor3 = Color3.fromRGB(85, 255, 85)
    else
        ToggleButton.Text = "Activar"
        ToggleButton.BackgroundColor3 = Color3.fromRGB(255, 85, 85)
        Status.Text = "Estado: Inactivo"
        Status.TextColor3 = Color3.fromRGB(255, 85, 85)
    end
end)

CloseButton.MouseButton1Click:Connect(function()
    ScreenGui:Destroy()
end)

spawn(function()
    while wait(checkInterval) do
        checkForAdmins()
    end
end)

print("Anti-ban cargado correctamente")
